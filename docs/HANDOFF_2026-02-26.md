# Solar OpenEO Handoff (2026-02-26)

## Purpose
This document is a practical handoff so future sessions can resume quickly without re-discovery.

## Current State
- Temporal mosaicing v1 is implemented and runnable.
- Earth Engine + Drive export path is working in `geospatial` conda env.
- Main output is a 13-band Sentinel-2 L1C mosaic with QA/provenance bands.
- Diagnostics workflow is implemented for single-location and batch multi-location QA.

## Environment
- Recommended env: `conda run -n geospatial ...`
- Earth Engine auth and initialization are required and already validated during development.

## Core Pipeline (Production)
Entrypoint:
- `mosaic_module.main.create_temporal_mosaic(...)`

High-level method:
1. Build planar AOI square in EPSG:3857 with `aoi_size_km`.
2. Load `COPERNICUS/S2_HARMONIZED`, join `COPERNICUS/S2_CLOUD_PROBABILITY`.
3. Build cloud mask from s2cloudless only:
   - `cloud_prob >= 65` (default)
   - morphology cleanup + minimum cloud component size filter (`min_cloud_px=4`)
4. Build `clear_mask`.
5. Score scenes by AOI clear fraction only.
6. Keep top candidate scenes (`top_n_scenes`, default 8).
7. SNIC segmentation on cloud-masked reference image.
8. Assign each cluster to best scene if clear fraction >= `clear_thresh` (default 0.8).
9. Hierarchical fallback assignment on finer projected patch grids.
10. Stitch 13 bands and feather seams.
11. Rescue pass for remaining gaps:
   - use top rescue candidates (`top_n_scenes_rescue`, default 10)
   - fill from per-pixel minimum `cloud_prob` source among rescue candidates
12. Export:
   - full product to Drive
   - RGB product (`*_rgb`) to same Drive folder

## Cloud Mask Method (Current)
File:
- `mosaic_module/cloud_mask.py`

Method:
- Cloud source: s2cloudless `cloud_prob` only (single-source policy).
- Default threshold: `cloud_prob_th = 65`.
- Morphology: `focal_min(1)` then `focal_max(radius=2 px)`.
- Speckle suppression: connected-component cloud size filter with default `min_cloud_px = 4`.
- Shadow stage: optional (`use_shadow=False` default).

Reason for this setup:
- Reduces single-pixel noise and permanent false-cloud artifacts.
- Keeps cloud policy simple and consistent across regions.

## Default Tuning (As Implemented)
- `clear_thresh = 0.8`
- `top_n_scenes = 8`
- `top_n_scenes_rescue = 10`
- `use_shadow_mask = False`
- `cloud_prob_th = 65` (inside `compute_clear_mask`)
- `min_cloud_px = 4` (inside `compute_clear_mask`)

## Output Bands
Spectral:
- `B1,B2,B3,B4,B5,B6,B7,B8,B8A,B9,B10,B11,B12`

QA/Provenance:
- `source_scene_id`
- `source_date` (`YYYYMMDD` int)
- `assigned_valid_mask`
- `fill_mode` (`1` means rescue fill applied)
- `rescue_cloud_prob` (cloud probability used in rescue-filled pixels)

## Diagnostics Workflows
Single location:
- `scripts/plot_mosaic_diagnostics.py`
- Shows top-N date rows + final quick preview mosaic.
- Supports:
  - `--aoi-km`
  - `--top-n`
  - `--thumb-px`
  - `--figure-dpi`
  - `--use-shadow`
  - `--max-scene-cloud-pct`

Batch (multi-location):
- `scripts/plot_mosaic_diagnostics_batch.py`
- Default list is now 10 non-urban locations.
- Writes PNGs and an HTML gallery under `--out-dir`.

Gallery utility:
- `scripts/build_diagnostics_gallery.py`

## Latest Batch Outputs
Folder:
- `outputs/diagnostics/batch10_singlemask_th65_rescue/`

Contains:
- 10 non-urban 3x3 km diagnostics PNGs
- `index.html` gallery

## Key Scripts
- Export test:
  - `scripts/test_temporal_mosaic_v1.py`
- Single diagnostics:
  - `scripts/plot_mosaic_diagnostics.py`
- Batch diagnostics:
  - `scripts/plot_mosaic_diagnostics_batch.py`
- Gallery builder:
  - `scripts/build_diagnostics_gallery.py`

## Resume Checklist (Next Session)
1. Read this file and `PROJECT_LOG.md`.
2. Activate/run via `conda run -n geospatial`.
3. Open latest gallery:
   - `outputs/diagnostics/batch10_singlemask_th65_rescue/index.html`
4. If changing mask behavior:
   - edit `mosaic_module/cloud_mask.py`
   - regenerate at least one single diagnostics plot
   - run a small batch (2-3 sites) before 10-site full batch
5. Before model-data production runs:
   - verify `fill_mode` fraction is acceptable
   - inspect `rescue_cloud_prob` distribution

## Suggested Next Improvements
- Add quantitative QC report per run:
  - % rescue-filled pixels
  - mean/percentile rescue cloud probability
  - scene fragmentation metric (source_scene_id entropy)
- Optional adaptive thresholding by biome/season.
- Persist selected intermediates for audit (`clusters`, `unassigned_mask`) when needed.
